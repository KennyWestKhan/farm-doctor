# Backend base URL. Leave blank for local/offline-only demo mode
# (everything stays in IndexedDB; no cloud calls).
VITE_API_URL=

# Supabase (phone OTP auth). Without these, app runs guest-only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
